// Placeholder for Lane C (frontend-engineer): the single vanilla-JS island described in
// architecture.md §11 — XHR upload with progress + cancel, copy-to-clipboard with the
// "הועתק" swap, status polling, deliveries polling. Intentionally inert for now; Lane B
// only needs `/assets/island.js` to resolve to a real file so `@fastify/static` has a
// valid root and the placeholder view templates' <script> tags don't 404.
